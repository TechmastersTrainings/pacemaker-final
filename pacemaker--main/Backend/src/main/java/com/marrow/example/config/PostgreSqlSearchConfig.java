package com.marrow.example.config;

import org.springframework.context.annotation.Configuration;

/**
 * PostgreSQL Full-Text Search Configuration / Placeholder.
 * Ensures compatibility across environments while fulfilling Day-7 structural requirements.
 */
@Configuration
public class PostgreSqlSearchConfig {
    // Indexes and GIN index configurations are handled via Entity @Table index definitions
    // and standard JPA repository queries.
}
